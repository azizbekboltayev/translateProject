from django.db import models

# class Dictionary(models.Model):
#         original = models.CharField('Original', max_length=200)
#         translation = models.CharField('Translation', max_length=200)
